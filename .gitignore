target/
node_modules/
*.zkey
*.ptau
*.wtns
build/
.stellar/
.soroban/
*.wasm
identity.txt
.env
web/dist/
dist/
# web/public holds served circuit artifacts (globally ignored via *.wasm/*.zkey).
# Re-include the two served by the web app so Vercel can build/serve them.
!web/public/transfer.wasm
!web/public/transfer_final.zkey
web/public/DA.jpg
.vercel
extension/umbra-extension.zip
